
namespace RTS
{
    public enum CursorState { Select, Move, Attack, PanLeft, PanRight, PanUp, PanDown,
    Gather, RallyPoint}

    public enum ResourceType { Minerals, Gas, Population, Unknown }
}
